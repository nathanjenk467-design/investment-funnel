# investment-funnel

Investment Architect funnel board.

**Run the pipe now** (do not wait for 8am):

```bash
python3 scripts/daily_pipe.py run-now
python3 scripts/daily_pipe.py run-now --frozen
python3 scripts/daily_pipe.py self-test
```

Start at [`docs/README.md`](docs/README.md). The runner is [`docs/daily-pipe.md`](docs/daily-pipe.md).

This repo does not publish the private local board (`/Users/max/Documents/Project Investment Thinktank/funnel/`). Do not add those files here.
