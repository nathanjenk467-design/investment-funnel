#!/usr/bin/env bash
# Engine proof: frozen Board fails; grow+keep writes ideas and 2–4 names.
set -euo pipefail
root="$(cd "$(dirname "$0")/.." && pwd)"
exec python3 "$root/scripts/daily_pipe.py" self-test "$@"
